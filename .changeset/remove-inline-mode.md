---
"znaki": major
---

Remove inline mode: the `mode` option is gone from the plugin and from the `local()` and `tabler()` sources, and every icon is now delivered through the sprite. The `IconMode` type is no longer exported.
